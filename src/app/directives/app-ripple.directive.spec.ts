import { AppRippleDirective } from './app-ripple.directive';

describe('AppRippleDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRippleDirective();
    expect(directive).toBeTruthy();
  });
});
